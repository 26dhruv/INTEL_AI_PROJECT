# Core Flask dependencies
Flask>=2.3.0
Flask-CORS>=4.0.0
Flask-SocketIO>=5.3.0
python-socketio>=5.8.0

# Database
pymongo>=4.5.0
dnspython>=2.2.1

# Basic image processing (lighter than full OpenCV)
opencv-python-headless>=4.8.0
Pillow>=10.0.0
numpy>=1.24.0

# Face recognition (this is the heavy one - consider alternatives)
face-recognition>=1.3.0
dlib>=19.24.0

# Authentication and security
bcrypt>=4.0.0
PyJWT>=2.8.0

# Utilities
python-dotenv>=1.0.0
python-dateutil>=2.8.0

# Production server
gunicorn>=21.0.0
eventlet>=0.33.0

# Optional: If you need pandas for data processing
# pandas>=2.0.0

# Optional: If you need torch/torchvision (very heavy - consider removing)
# torch>=2.0.0
# torchvision>=0.15.0
# ultralytics>=8.0.0 